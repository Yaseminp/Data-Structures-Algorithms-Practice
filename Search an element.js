//Search an element in an array where difference between adjacent elements is 1.


  function findElement(arr, ele, low=0, high= arr.length-1) {
    //Traverse the given array starting from leftmost
    
    let mid = (low+high)/2
    if (arr[mid]===ele)
    return mid;
    if (arr[mid] > arr[m+1])
     
  }
  
  findElement([8, 7, 6, 7, 6, 5, 4, 3, 2, 3, 4, 3],3);

  